let products=[],cart=JSON.parse(localStorage.getItem('micha_cart')||'[]');const money=n=>'$'+n.toLocaleString('es-AR');async function init(){products=await(await fetch('products.json')).json();renderFilters();render();updateCart()}function renderFilters(){let cats=['Todos',...new Set(products.map(p=>p.category))];filters.innerHTML=cats.map((c,i)=>`<button class="chip ${i?'':'active'}" onclick="filterCat('${c}',this)">${c}</button>`).join('')}function render(cat='Todos'){grid.innerHTML=products.filter(p=>cat==='Todos'||p.category===cat).map(p=>`<article class="card"><div class="photo">${p.name}</div><p class="eyebrow">${p.category}</p><h3>${p.name}</h3><p>${money(p.price)}</p><button class="btn" onclick="add(${p.id})">Agregar</button></article>`).join('')}function filterCat(c,b){document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(c)}function add(id){let x=cart.find(i=>i.id===id);x?x.q++:cart.push({id,q:1});save();openCart()}function save(){localStorage.setItem('micha_cart',JSON.stringify(cart));updateCart()}function updateCart(){cartCount.textContent=cart.reduce((a,x)=>a+x.q,0);let sum=0;cartItems.innerHTML=cart.map(x=>{let p=products.find(y=>y.id===x.id);if(!p)return'';sum+=p.price*x.q;return `<div class="cart-row"><span>${p.name} × ${x.q}</span><b>${money(p.price*x.q)}</b></div>`}).join('')||'<p>Tu carrito está vacío.</p>';total.textContent=money(sum)}function openCart(){drawer.classList.add('open');overlay.classList.add('open')}function closeCart(){drawer.classList.remove('open');overlay.classList.remove('open')}function checkout(){document.getElementById('checkout').hidden=false}function createOrder(){let n='TM-'+Date.now().toString().slice(-7);orderResult.innerHTML=`<p><b>Pedido demo ${n}</b><br>Estado: Pendiente de pago.<br>Esta versión todavía no envía ni cobra pedidos reales.</p>`}init();