let products=[],cart=JSON.parse(localStorage.getItem('micha_cart')||'[]');const $=id=>document.getElementById(id),money=n=>'$'+n.toLocaleString('es-AR');async function init(){products=await(await fetch('products.json')).json();renderFilters();render();updateCart()}function renderFilters(){let cats=['Todos',...new Set(products.map(p=>p.category))];$('filters').innerHTML=cats.map((c,i)=>`<button class="chip ${i?'':'active'}" onclick="filterCat('${c}',this)">${c}</button>`).join('')}function render(cat='Todos'){$('grid').innerHTML=products.filter(p=>cat==='Todos'||p.category===cat).map(p=>`<article class="card"><div class="photo"></div><div class="card-info"><span class="eyebrow">${p.category}</span><h3>${p.name}</h3><span class="price">Precio a confirmar</span><button class="add" onclick="add(${p.id})">Agregar a la bolsa</button></div></article>`).join('')}function filterCat(c,b){document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(c)}function add(id){let x=cart.find(i=>i.id===id);x?x.q++:cart.push({id,q:1});save();openCart()}function save(){localStorage.setItem('micha_cart',JSON.stringify(cart));updateCart()}function updateCart(){$('cartCount').textContent=cart.reduce((a,x)=>a+x.q,0);let priced=0;$('cartItems').innerHTML=cart.map(x=>{let p=products.find(y=>y.id===x.id);if(!p)return'';if(p.price)priced+=p.price*x.q;return `<div class="cart-row"><span>${p.name} × ${x.q}</span><b>${p.price?money(p.price*x.q):'A confirmar'}</b></div>`}).join('')||'<p>Tu bolsa está vacía.</p>';$('total').textContent=priced?money(priced):'A confirmar'}function openCart(){$('drawer').classList.add('open');$('overlay').classList.add('open')}function closeCart(){$('drawer').classList.remove('open');$('overlay').classList.remove('open')}function checkout(){$('checkout').hidden=false}function createOrder(){let n='MC-'+Date.now().toString().slice(-7);$('orderResult').innerHTML=`<p><b>Pedido de prueba ${n}</b><br>Esta versión todavía no envía ni cobra pedidos reales.</p>`}init();